interface stateConfig {
  volume: number;
  memory: boolean;
  simon: boolean;
}

const state: stateConfig = {
  volume: 1,
    memory: true, // can play
    simon: true, // can play
};

export { state };
